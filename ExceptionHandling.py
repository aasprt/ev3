#Non è richiesta l'importazione di alcuna libreria
try:
    #Codice dal quale evitare l'interruzione del programma al raise di un errore
    #Implementabile all'inizio del ciclo principale
except:
    #Codice da eseguire in caso di errore
    #Possibile l'utilizzo di "continue" in un ciclo
else:
    #Istruzione opzionale
    #Codice che si esegue solo nel caso in cui non vengano riscontrati errori
finally:
    #Istruzione opzionale
    #Codice che si esegue in ogni caso, anche a seguito dell'alterazione dell'iterazione del programma nell'istruzione "except"
